eval(File.read(File.dirname(__FILE__) + "/../common_white_album_dependencies.rb"))

gem "white_album_core", path: "../core"

# This allows to a system like CDN to manage our images
gem "image_system", github: "BenjaminMedia/image_system"

gemspec
